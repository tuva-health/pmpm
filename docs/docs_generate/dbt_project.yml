name: 'pmpm_input'
version: '1.0.0'
config-version: 2

profile: 'tuva'


dispatch:
  - macro_namespace: dbt
    search_order: [ 'pmpm', 'dbt']

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
docs-paths: ["documents"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
