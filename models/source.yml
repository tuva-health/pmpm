version: 2

sources:
  - name: "{{ var('source_name') }}"
    database: "{{ var('input_database') }}"
    schema: "{{ var('claims_input_schema') }}"
    tables:
      - name: pharmacy_claim
      - name: medical_claim
      - name: eligibility