version: 2

sources:
  - name: "{{ var('source_name') }}"
    database: "{{ var('input_database') }}"
    schema: "{{ var('input_schema') }}"
    tables:
      - name: coverage
      - name: encounter
  - name: "{{ var('source_name') }}"
    database: "{{ var('input_database') }}"
    schema: "{{ var('pharmacy_schema') }}"
    tables:
      - name: pharmacy_claim